spring:
  datasource:
    url: jdbc:mysql://localhost:3306/Artist_DB
    username: root
    password: int
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      format-sql: true
    database: mysql
    database-platform: org.hibernate.dialect.MySQLDialect